# Output

Optional local outputs for IBKR Client.

Typical file:

- `connectivity.json` — local result from `connectivity_check.py --output ...`.

Runtime outputs are ignored by git except this README.
