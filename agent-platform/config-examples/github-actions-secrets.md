# GitHub Actions Secrets

Required for `.github/workflows/vps-deploy.yml`:

- `VPS_HOST`
- `VPS_USER`
- `VPS_SSH_PRIVATE_KEY`
- `VPS_SSH_HOST_KEY`

These values are not stored in the repo. Update them in GitHub when moving to a new VPS.
