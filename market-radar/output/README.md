# Output

Runtime outputs for Market Radar.

Typical files:

- `market_radar_state.json` — RSS deduplication state.
- `market_radar_cron.log` — scheduler/wrapper logs.

These are runtime artifacts and are ignored by git.
